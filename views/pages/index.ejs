<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="A small app to send messages, created to learn authentication">
    <meta name="author" content="Ignacio Gandur">
    <title>Homepage</title>
    <link rel="stylesheet" href="/css/variables.css">
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="/css/navbar.css">
    <link rel="stylesheet" href="/css/header.css">
    <link rel="stylesheet" href="/css/classes.css">
    <link rel="stylesheet" href="/css/index.css">
    <link rel="stylesheet" href="/css/animations.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Sharp:opsz,wght,FILL,GRAD@20,100,0..1,-50" />
    <script defer src="/js/utilities/noTranslateGoogleIcons.js"></script>
    <script defer src="/js/utilities/toggleMobileNavbar.js"></script>
</head>
<body>
    <%- include('../partials/navbar.ejs',  {currentPath}) %>
    <main>
        <%- include('../partials/header.ejs', {pageTitle: 'Homepage | Messages'}) %>
        <% if(locals.validationErrors) { %>
            <div class="errors">
                <p class="title">
                    Something is wrong with the following input fields:
                </p>
                <% validationErrors.forEach((error) => { %>
                    <li>
                        <%= error.msg %>
                    </li>
                <% }) %>
            </div>
        <% } %>
        <% if(locals.messages.length !== 0) { %>
            <div class="filters">
                <form class="form-field select-form" action="/sort" method="GET">
                    <label class="label" for="sortBy">Sort messages by</label>
                    <div class="input-wrapper">
                        <span class="material-symbols-sharp input-icon">schedule</span>
                        <select name="sortBy" id="sortBy">
                            <option value="DESC" selected>Newest</option>
                            <option value="ASC">Oldest</option>
                        </select>
                        <button type="submit">
                            <span class="material-symbols-sharp">arrow_forward</span>
                        </button>
                    </div>
                </form>
            </div>
        <% } %>
        <% if(locals.sortingMessages) { %>
            <p class="sort-results-message">
                <%= sortingMessages %>
            </p>
        <% } %>
        <%- include('../partials/render-message.ejs',{messages}) %>
        <%- include('../partials/messages-bg.ejs') %>
    </main>
</body>
</html>
